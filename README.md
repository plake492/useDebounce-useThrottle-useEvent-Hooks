### **Build Your Own `useDebounce` + `useThrottle` + `useEvent` Hooks**

Focus on:

- Preventing stale closures
- Correct cleanup
- Race-condition-safe async behavior

**Skills:** closures, stable references, event behavior, concurrency.